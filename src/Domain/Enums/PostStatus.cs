namespace Zemoga_Test.Domain.Enums;

public enum PostStatus
{
    Draft = 0,
    Pending = 1,
    Approved = 2,
    Rejected = 3,
}

public enum Role
{
    Public = 0,
    Writer = 1,
    Editor = 2,
}
